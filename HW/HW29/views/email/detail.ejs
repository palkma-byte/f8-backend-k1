<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Detail Page</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
</head>
<body>

<div class="container mt-5">
    <h2>Email Detail</h2>
    <hr>

    <!-- Email Content -->
    <div class="card">
        <div class="card-body">
            <h5 class="card-title"><%= emailInfo.title %></h5>
            <p class="card-text">
                <%= emailInfo.content %></p>
        </div>
    </div>

    <!-- Additional Details -->
    <div class="mt-4">
        <h4>Additional Details</h4>
        <ul>
            <li><strong>Client:</strong> <%= emailInfo.clientEmail %></li>
            <li><strong>Sent Time:</strong> <%= moment(emailInfo.createdAt).format('MMMM Do YYYY, h:mm:ss a')%></li>
            <li><strong>Trạng thái:</strong> <%= emailInfo.status === 1 ? "Đã đọc" : "Chưa đọc" %></li>
            <!-- Add more details as needed -->
        </ul>
    </div>
    <div class="mt-2"> 
        <a href="/" class="btn btn-primary">Back to home</a>
        <a href="/history" class="btn btn-primary">Back to history page</a>
    </div>
</div>


</body>
</html>
